# 문자열에서 숫자 추출하기
```java
import java.util.*;
import java.util.stream.*;
String[] str = Stream.of(String.valueOf(my_string).split("[^0-9]"))
            .filter(s -> !s.isEmpty()) 
            .toArray(String[]::new);
```
